# The URL the site will be built for
base_url = "https://tkggunter.github.io/"
#base_url = "http://0.0.0.0:8000"

# Whether to automatically compile all Sass files in the sass directory
compile_sass = false

# Whether to build a search index to be used later on by a JavaScript library
build_search_index = true

theme = "hyde"

title = "Thoth Gunter"
description = "programming, machine learning, physics"

[markdown]
# Whether to do syntax highlighting
# Theme can be customised by setting the `highlight_theme` variable to a theme supported by Zola
highlight_code = false

[extra]
# Put all your custom variables here
hyde_theme = "theme-base-0d"
hyde_links = [
    {url = "https://github.com/TKGgunter", name = "github"},
    {url = "https://tkggunter.github.io/projects", name = "projects"},
    {url = "https://tkggunter.github.io/presentations", name = "presentations"},
]
